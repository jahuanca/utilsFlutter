

enum ButtonType{
  base,
  ghost,
  link,
}

enum ButtonStyle{
  //If it is a base type, the background Color, textColor and iconColor defined by the user are taken.
  base,
  success,
  danger,
  info,
  alert,
  dark,
  outline,

}