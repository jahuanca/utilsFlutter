/// Duracion de 1 día.
const Duration oneSecondDuration = Duration(seconds: 1);
/// Duracion de 1 día.
const Duration oneDayDuration = Duration(days: 1);

/// Duración de una semana.
const Duration oneWeekDuration = Duration(days: 7);

/// Duración de 15 días.
const Duration fortnightDuration = Duration(days: 15);

/// Duración de medio anio o 180 días
const Duration halfYearDuration = Duration(days: 180);

/// Duración de 1 anio o 360 días
const Duration oneYearDuration = Duration(days: 360);