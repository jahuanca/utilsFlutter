import 'package:flutter/material.dart';
const EdgeInsets defaultPadding = EdgeInsets.all(8.0);
const EdgeInsets noPadding = EdgeInsets.all(0);
final Widget emptyWidget = Container();
const maxLengthDefault = 20;